<resources xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Base application theme. -->
    <style name="Theme.Nordside_mobile" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#015DA8</item>
        <item name="colorPrimaryVariant">#587DFF</item>
        <item name="colorOnPrimary">@color/greyBack</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/grey</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorOnPrimary</item>
        <item name="android:windowTranslucentNavigation">?attr/colorOnSecondary</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="GreenButtonThemeOverlay">
        <item name="colorPrimary">@color/green</item>
    </style>

    <style name="BigButton"
        parent="Widget.MaterialComponents.Button">
        <item name="materialThemeOverlay">@style/GreenButtonThemeOverlay</item>
        <item name="android:layout_height">54dp</item>
        <item name="android:layout_width">0dp</item>
        <item name="cornerRadius">20dp</item>
        <item name="textAllCaps">false</item>
        <item name="layout_constraintWidth_percent">0.9</item>
    </style>

    <style name="ProductCard">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_marginEnd">5dp</item>
        <item name="android:layout_marginStart">5dp</item>
        <item name="android:layout_height">120dp</item>
        <item name="cardElevation">2dp</item>
        <item name="cardCornerRadius">10dp</item>
    </style>

    <style name="BigTextType">
        <item name="android:textSize">18sp</item>
    </style>

</resources>